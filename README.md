# despliegue

This repository contains `app.py` for deployment and related files.

Quick start

1. Create a Python virtual environment and install dependencies.
2. Run the app locally: `python app.py`.

To publish this repository to GitHub, initialize git, commit files, then push. See the example commands in the project root or ask me to run them for you.
